_min_copier_version: v6.0.0b0
title:
  type: str
priority:
  type: int
  default: 0
slug:
  type: str
  default: "{{  title.lower().replace(' ', '-')  }}"
tag1:
  type: str
tag2:
  type: str
tag3:
  type: str
